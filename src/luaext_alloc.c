/*
 * luaext — placeholder for the alloc subsystem.
 *
 * The contract lives in luaext_alloc.h; this file exists so the module links
 * while the implementation is being written. Replace it wholesale rather than
 * extending it.
 */

#include "luaext_alloc.h"
