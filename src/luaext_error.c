/*
 * luaext — placeholder for the error subsystem.
 *
 * The contract lives in luaext_error.h; this file exists so the module links
 * while the implementation is being written. Replace it wholesale rather than
 * extending it.
 */

#include "luaext_error.h"
