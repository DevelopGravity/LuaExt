/*
 * luaext — placeholder for the config subsystem.
 *
 * The contract lives in luaext_config.h; this file exists so the module links
 * while the implementation is being written. Replace it wholesale rather than
 * extending it.
 */

#include "luaext_config.h"
